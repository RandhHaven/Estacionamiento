@model List<Estacionamiento.Models.Empleado>

@{
    ViewData["Title"] = "Detalle De Empleados";
}

@using (Html.BeginForm("ViewEmpleados", "Home", FormMethod.Get))
{
    <div>
        <h1> Sistema Meteorologico </h1>
    </div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th style="width:100px;">Legajo</th>
                <th style="width:100px;">Nombre</th>
                <th style="width:100px;">Apellido</th>
                <th style="width:200px;">Edad</th>
                <th style="width:200px;">Celular</th>
                <th style="width:200px;">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var sist in Model)
            {
            <tr>
                <td>@sist.Legajo</td>
                <td>@sist.Nombre</td>
                <td>@sist.Apellido</td>
                <td>@sist.Edad</td>
                <td>@sist.NroCelular</td>
                <td>
                    <a class="btn-primary" href="~/Home/ViewEditarEmpleado/@sist.IdEmpleado" title="Editar">
                        <i class="glyphicon glyphicon-pencil"></i>
                    </a>
                    <a class="btn-primary" title="Eliminar">
                        <i class="glyphicon glyphicon-pencil"></i>
                    </a>
                </td>
            </tr>
            }
        </tbody>
    </table>
    <div>
        <a class="btn-primary" href="~/Home/ViewCrearEmpleado" title="Crear">
            <i class="glyphicon glyphicon-pencil"></i>
        </a>
    </div>
}
